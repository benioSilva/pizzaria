"=" vc tá atribuindo um valor
"==" vc tá comparando um valor

variaveis

let = variavel local
var = variavel global 
const = variavel que nao aceita alteração

objetos
{
indice: valor,
indice: valor
}
posso colocar um objeto dentro de outro objeto

arrays
pode ter outro array dentro, variaveis, objetos, numeros, strings...

condicionais

-if (se alguma coisa)
-else (senão)
-else if (senão alguma coisa)
-swtich (uma lista de valores vai comparando até achar o valor)